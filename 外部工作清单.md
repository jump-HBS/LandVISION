# 外部工作清单 —— 在项目文件夹之外需要做的事

> 本项目文件夹内已经包含了**可直接运行的代码**（后端 Demo 模式 + 前端页面）。
> 但要让项目达到"完整可用"状态，以下工作必须在**本文件夹之外**完成。
> 按依赖顺序排列，每项都标注了：必要性、怎么做、怎么验证。

---

## 0. 状态总览

| 项 | 状态 | 说明 |
|---|---|---|
| Python 3.14 + venv + 后端依赖 | ✅ 已完成 | `venv/` 已含 fastapi/uvicorn/sqlalchemy/geoalchemy2/shapely 等 |
| Node.js + npm | ✅ 已完成 | 已检测到 Node v24 / npm 11 |
| PostgreSQL + PostGIS | ✅ 已完成 | 数据库 `landvision` 已建库并导入省市县行政区与业务演示数据 |
| 桌面工具（pgAdmin / DBeaver） | ⬜ 可选 | 见第 2 节 |
| Git | ⬜ 未安装 | 见第 3 节 |
| Docker Desktop | ⬜ 未安装 | 见第 4 节（部署阶段才需要） |
| 地图底图样式 | ✅ 已内置 | OpenFreeMap（免费），见第 5 节可替换 |
| 县域行政区 SHP | ⬜ 可选 | 见第 7 节（平台已内置全国省市县数据，SHP 作为补充数据源） |

---

## 1. 安装 PostgreSQL 16 + PostGIS（模块一，已完成）

> 已安装并完成初始化：数据库 `landvision`、用户 `postgres`，省市县行政区与演示业务数据均已入库，
> 后端以 **POSTGIS 模式**运行；数据库连接失败时会自动降级为 Demo 模式（内存数据）。

**Windows 步骤（首次安装参考）：**
1. 到 https://www.postgresql.org/download/windows/ 下载 **PostgreSQL 16.x** 安装包（EDB 版）。
2. 安装时记住：端口 `5432`、超级用户密码（建议 `postgres`）。
3. 安装完成后打开 **Stack Builder**，选择同版本 PostgreSQL，勾选 **PostGIS 3.x Bundle** 并安装。
4. 验证：
   ```sql
   SELECT version();            -- 应显示 PostgreSQL 16.x
   SELECT PostGIS_Version();    -- 应显示 3.x 版本号
   ```

---

## 2. 安装数据库图形工具（可选但强烈建议）

- **pgAdmin 4**：PostgreSQL 自带（安装 PostgreSQL 时默认包含）。
- 或 **DBeaver Community**（https://dbeaver.io/，跨库通用，支持空间数据预览）。

**用途：** 执行 `database/` 下的 SQL 脚本、查看执行计划（`EXPLAIN ANALYZE`）、调试空间查询。

---

## 3. 安装 Git（版本管理，长期收益最大）

- 下载：https://git-scm.com/download/win
- 安装后在项目根目录执行：
  ```powershell
  git init
  git add .
  git commit -m "LandVISION 初始代码"
  ```
- 可选：注册 GitHub/Gitee 账号，把仓库推送到远端备份。

---

## 4. 安装 Docker Desktop（模块七，部署阶段）

- 下载：https://www.docker.com/products/docker-desktop/
- 安装后启动，验证：`docker version`、`docker compose version`
- 然后在项目根目录执行：
  ```powershell
  cd deploy
  docker compose up -d --build
  ```
  即可一键启动 数据库 + 后端 + 前端 三服务。

---

## 5. 确认/替换地图底图样式（模块五）

- 本项目 `MapView.vue` 默认使用 **OpenFreeMap**（免费、无需 token）：`https://tiles.openfreemap.org/styles/liberty`
- 如果加载缓慢或想用其他底图，可替换为：
  - 高德/天地图（需申请 key）
  - MapTiler（https://cloud.maptiler.com/，免费额度，需要 api key）
  - 自建瓦片服务
- 替换位置：`frontend/src/components/MapView.vue` 中的 `style` 配置。

---

## 6. 浏览器与编辑器

- 推荐浏览器：Chrome / Edge（F12 开发者工具是调试主战场）。
- 编辑器：VS Code（项目已含 `.vscode/settings.json`），推荐安装插件：
  - **Python**（ms-python.python）
  - **Vue - Official**（Volar）
  - **ESLint** / **Prettier**
  - **SQLTools** 或 SQL 插件（执行 database 下的脚本）

---

## 7. 下载县域行政区 SHP（天地图，可选）

平台已内置**全国省市县三级行政区**数据（34 省 + 346 市 + 2883 县），行政区选择器可直接检索定位。
如需使用权威部门发布的最新县域矢量或用作分析范围，可从天地图获取：

1. 打开全国地理信息资源目录服务系统 https://www.webmap.cn/ （天地图·注册登录）
2. 进入"**数据资源 → 行政区划**"，按省/市找到所需**县（区）**级别数据
3. 下载 **Shapefile（shp）** 格式压缩包，解压确认包含 `.shp/.shx/.dbf/.prj` 四个文件，**重新打包为 zip**
4. 坐标系检查（很重要）：若 `.prj` 不是 WGS84（CGCS2000 亦为经纬度，可直接用），
   用 GDAL 转换（GDAL 为可选工具，仅需转换时安装）：
   ```powershell
   gdalwarp -t_srs EPSG:4326 原文件.shp 输出.shp
   ```
5. 使用方式：
   - 作为**分析范围**：各分析页"导入 SHP 范围"直接上传（不落库）；
   - 作为**行政区数据**：`POST /api/regions/import`（level=county，parent_code=上级市代码）；
   - 作为**业务地块**：地块管理页 / 用地转移矩阵页上传 SHP 批量入库。

---

## 8. 建议的"外部 → 内部"对接顺序

```
第 1 周：确认 PostgreSQL+PostGIS 连接 → 后端 POSTGIS 模式 → 逐个体验四大分析模块
第 2 周：装 Git → 提交代码 → 读《项目实现框架.md》理解空间算法
第 3 周：下载县域 SHP → 在分析页划定范围 → 生成报告与问题台账 CSV
第 4 周：装 Docker Desktop → 学模块七，部署 deploy/ 目录
```

每完成一项外部安装，回到本项目做对应验证（见各节"验证"），形成"装一步、学一步、练一步"的闭环。
